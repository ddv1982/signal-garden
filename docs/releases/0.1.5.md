# Release 0.1.5 — sign off the mobile lens path

Record the follow-up release for the mobile active-lens fix now that Playwright browsers and system dependencies are available and the targeted mobile regression plus the broader e2e suite are green.

## Highlights

- Confirms the active mobile lens object no longer overlaps the pet interaction target.
- Keeps `data-active-lens-x` and `data-active-lens-y` tied to the same hit circle that Phaser makes interactive.
- Records fresh mobile Chrome and full-suite e2e sign-off in the QA report.
- Gives the local pet-debug visual review enough time to complete on slower local browsers without changing CI coverage.

## What changed

- Release metadata:
  - `package.json`
  - `docs/releases/README.md`
  - `docs/releases/0.1.5.md`
  - Bumps the package release line to `0.1.5` and adds this note to the release index.
- Mobile garden input:
  - `src/game/gardenLayout.ts`
  - `src/game/GardenGame.ts`
  - Compact active lens placements stay clear of the pet ellipse, and DOM-exposed active coordinates come from the exact Phaser hit target.
- QA and tests:
  - `src/game/__tests__/gardenLayout.test.ts`
  - `e2e/garden-loop.spec.ts`
  - `docs/qa/report/index.html`
  - `docs/qa/report/assets.css`
  - Unit coverage now checks 390px mobile `word` and `observer` hit targets; the QA report shows current YES sign-off; the local pet-debug harness timeout is long enough for the full suite to pass locally.

## Lore

Constraint: Release history should record the moment the mobile active-lens fix became browser-verified, not just unit-verified.
Rejected: Reuse `0.1.4` | The earlier release note included the intended fix, but this branch adds the fresh Playwright sign-off and should remain auditable as its own release.
Rejected: Ship without the full e2e rerun | The original blocker was browser interaction geometry, so the release needs browser evidence.
Confidence: high
Scope-risk: narrow
Directive: Let the visible active lens own the tap target, and let the release notes own the proof.
Tested: `corepack pnpm exec playwright install chromium`; `corepack pnpm exec playwright install-deps chromium`; `corepack pnpm run typecheck`; `corepack pnpm run test`; `corepack pnpm exec playwright test e2e/garden-loop.spec.ts:79 --project=mobile-chrome`; `corepack pnpm run e2e`; `corepack pnpm run build`.
Not-tested: GitHub Actions production deploy has not run for this tag yet.
